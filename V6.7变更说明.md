# V6.7 变更说明

## 版本定位

`V6.7` 基于 `mnt_bot V 6.6 plus.py` 形成，核心目标是把今天确认有效的 `ADK pair-score peak-decay overlay` 正式并入策略主脚本。

这次不是继续强化旧 `DD gate`，而是把研究中更有效的“动量衰减”逻辑变成默认正式口径。

## 本次正式纳入的改动

### 1. ADK 新增信号衰减 overlay

在 `run_dk_strategy()` 之后，新增一层正式风控：

1. 跟踪当前 `Top-1 pair` 的活动分数峰值。
2. 当当前分数相对本轮峰值衰减到阈值以下时，次日进入降风险。
3. 当分数恢复到恢复阈值以上时，回到满仓。
4. 恢复后必须先创出新的 trade peak，才允许下一次再次触发。
5. 如果 `Top-1 pair` 切换，则视为新 trade，旧过滤状态不继承。

对应正式参数固定为：

- `CN_DK_PAIR_SCORE_DECAY_ENABLED = True`
- `CN_DK_PAIR_SCORE_DECAY_RATIO = 0.40`
- `CN_DK_PAIR_SCORE_RECOVERY_RATIO = 0.70`
- `CN_DK_PAIR_SCORE_DERISK_SCALE = 0.0`

### 2. 旧 DD gate 保留，但默认关闭

`V6.7` 没有删掉 `apply_dk_drawdown_risk_gate()`，只是把默认开关改为：

- `CN_DK_RISK_GATE_ENABLED = False`

原因很简单：按今天同口径实测，旧 `DD gate` 在当前样本里没有优于“只用新 overlay”的版本，更多是收益换回撤，而不是更优默认解。

### 3. 正式 helper 并入主脚本

新增正式函数：

- `_extract_active_pair_score()`
- `apply_dk_pair_score_peak_decay_overlay()`

这样后续如果还要继续扫参数，不需要再依赖归档研究脚本，可以直接在正式版本上做。

## 默认风险栈顺序

`V6.7` 的 `Sub-A-DK` 风险栈顺序为：

1. `run_dk_strategy()` 生成原始 ADK 收益与 VolScale
2. `pair-score peak-decay overlay` 做信号衰减控制
3. 若手动重新打开 `CN_DK_RISK_GATE_ENABLED`，再叠加旧 `DD gate`

也就是说，`V6.7` 的默认口径是：

- `新 overlay 开`
- `旧 DD gate 关`

## 当前实测结论

本次采用的正式候选口径来自今天同一套本地数据与统一样本测试。

### ADK 子策略层

同口径基线：

- `baseline_no_dd_gate`: 年化 `30.1971%`，最大回撤 `-22.4089%`

`V6.7` 默认新参数：

- `pair_score_decay_only (40% / 70% / 0.0)`: 年化 `30.6294%`，最大回撤 `-20.7726%`

增量为：

- 年化 `+0.4323%`
- 最大回撤改善 `+1.6363%`

分窗口表现：

- `1Y`: `42.2439% / -15.7077%`
- `3Y`: `29.6304% / -20.7726%`
- `5Y`: `32.7310% / -20.7726%`

### 整体组合层

组合层数字沿用今天已完成的 `adk_pair_score_combo_impact.csv` 研究结果，用来说明把 `Sub-A-DK` 换成 `V6.7` 默认口径后的组合影响方向。

当前组合权重维持：

- `Sub-A 15% / Sub-A-DK 25% / Sub-B 40% / Sub-C 20%`

只把 `Sub-A-DK` 替换为 `V6.7` 默认口径后的组合结果：

- 基线组合：年化 `28.5862%`，最大回撤 `-14.3633%`
- `V6.7` 组合：年化 `29.2729%`，最大回撤 `-13.6675%`

增量为：

- 年化 `+0.6867%`
- 最大回撤改善 `+0.6958%`

## 对 V6.7 的一句话定义

`V6.7` = `V6.6` + `ADK pair-score peak-decay overlay 正式化`，并把旧 `DD gate` 降为可选项而非默认项。
