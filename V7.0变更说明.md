# V7.0 变更说明

生成时间：2026-04-22

## 版本定位

V7.0 基于 V6.8.3 正式升级，策略计算逻辑与 V6.8.3 保持一致；本次升级主要是把已经审核通过的 6.8.3 作为新的正式主版本，并补齐信号查询、实时信号、参数、实时参数里的新增规则展示。

## 主要规则

### Sub-A

- 保留首日阴线建仓过滤。
- 启用同向过热防守：
  - 触发阈值：36%
  - 恢复阈值：34%
  - 触发后仓位：0.0x
- `信号`、`实时信号`、`参数`、`实时参数` 均展示该过热防守状态和阈值。

### Sub-A-DK

- 保留 V6.8.2 的 ADK 规则：
  - Score 衰减防守
  - 同向过热防守 22% / 18%
  - 触发后仓位 0.0x
- `信号`、`实时信号`、`参数`、`实时参数` 均展示 ADK 同向过热防守状态和阈值。

### Sub-B

- 保留 V6.8.1 的杠杆资产池：
  - 仅 QQQ / GLD 可加杠杆，对应实盘展示 QQQM / GLDM
  - TLT 不再属于可加杠杆资产
- 保留修正后的 Model B：
  - `scale <= 1`：所有风险资产等比例缩减
  - `scale > 1`：仅 QQQ / GLD 按自身原始权重放大
  - 不再把其他 ETF 的杠杆缺口转嫁给 QQQ / GLD
- `信号`、`实时信号`、`参数`、`实时参数` 均明确展示该规则。

## 审核与验证

### 静态检查

- V7.0 文件公开标识已更新：
  - Poe 名称：`Strategy-Signal-V70`
  - 文档字符串：`V7.0`
  - 欢迎页：`Strategy Signal V7.0`
  - 主类：`CombinedStrategyV70`
  - 运行入口：原生 `poe` 命名空间 + `CombinedStrategyV70().run()`
- V7.0 运行时清理：
  - 移除 `import fastapi_poe as poe`，不再覆盖 Poe 原生命名空间
  - 删除本地兼容层和 ASGI Bot wrapper
  - `fastapi_poe.types` 仅保留 `SettingsResponse`
  - `poe.update_settings(_BOT_SETTINGS)` 直接调用原生 API

### 测试

已执行：

```powershell
python -m unittest tests.test_v683_query_disclosures
python -m unittest discover -s tests
python -m py_compile "mnt_bot V 6.8.3 plus.py" "mnt_bot V 7.0 plus.py" tests/test_v683_query_disclosures.py
```

结果：

- 查询披露测试：通过
- 全量 unittest：通过，15 个测试
- 语法编译：通过

### 真实数据入口验证

使用 V7.0 自身正式入口：

```python
_fetch_data + _run_strategies
```

验证结果：

- `cn_close`：3859 行，至 2026-04-22
- `cn_dk_close`：3858 行，至 2026-04-22
- `us_rot_close`：4755 行，至 2026-04-22
- `us_open_count`：19
- `QQQ / GLD / BIL` open 数据存在
- Sub-A 过热字段存在
- ADK 过热字段存在
- Sub-B VolReg 字段存在
- `US_ROT_FUTURES = GLD, QQQ`
- TLT 不在可加杠杆资产池

### V7.0 与 V6.8.3 一致性

在同一份 V7.0 正式拉取数据上，V7.0 与 V6.8.3 的输出一致：

- Sub-A 日收益最大差异：0
- ADK 日收益最大差异：0
- Sub-B 日收益最大差异：0
- Sub-C 净值最大差异：0

结论：V7.0 是 V6.8.3 的正式主版本升级，不改变策略计算结果。

## 已知非阻断事项

- 运行时出现 pandas `resample("M")` 的 FutureWarning，提示未来建议改为 `ME`。这不影响当前运行结果，后续可单独做兼容性清理。
