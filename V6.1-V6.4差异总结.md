# V6.1-V6.4 差异总结

本文基于当前工作区中的四个脚本整理：

- `mnt_bot V 6.1 plus.py`
- `mnt_bot V 6.2 plus.py`
- `mnt_bot V 6.3 plus.py`
- `mnt_bot V 6.4 plus.py`

## 一页看懂

| 版本 | Sub-A 国债避险资产 | 美股最小调仓阈值 | Combined 权重 | 备注 |
|:-|:-|:-:|:-|:-|
| V6.1 | 10Y国债 | 15% | 15 / 15 / 40 / 30 | V6 系列基线版本 |
| V6.2 | 10Y国债 | 15% | 15 / 25 / 40 / 20 | 提高 Sub-A-DK，降低 Sub-C |
| V6.3 | 10Y国债 | 15% | 20 / 30 / 50 / 0 | Combined 中取消 Sub-C |
| V6.4 | 30Y国债 | 25% | 15 / 15 / 40 / 30 | 恢复 Sub-C，并把 Sub-A 避险资产改为 30Y 国债 |

权重顺序固定为：

`Sub-A / Sub-A-DK / Sub-B / Sub-C`

## 核心差异

### 1. V6.1

V6.1 是当前这一套架构的基线版本。

- Sub-A 采用“乖离动量 + R²过滤 + 国债轮动 + 波动率缩放”。
- Sub-A 的避险资产是 `10Y国债`。
- 美股最小调仓阈值为 `15%`。
- Combined 权重为 `15 / 15 / 40 / 30`。

对应代码位置：

- `CN_BOND_CODE` / `CN_BOND_NAME`
- `COMBINED_WEIGHTS`
- `US_ROT_MIN_TURNOVER`

## 2. V6.2

V6.2 相对 V6.1，主要变化不是策略引擎本身，而是组合资金分配。

- `Sub-A` 不变，仍是 `10Y国债` 避险。
- `US_ROT_MIN_TURNOVER` 仍为 `15%`。
- Combined 权重从 `15 / 15 / 40 / 30` 改成 `15 / 25 / 40 / 20`。

实际含义：

- 增加了 `Sub-A-DK` 在组合中的占比。
- 降低了 `Sub-C` 在组合中的占比。
- 如果你是按 Combined 自动分配资金，V6.2 相比 V6.1 会把更多资金分给 A-DK，较少分给 C。

## 3. V6.3

V6.3 的主要变化集中在 Combined 组合定义，而不是 Sub-A 主策略公式。

- `Sub-A` 仍然使用 `10Y国债`。
- `US_ROT_MIN_TURNOVER` 仍为 `15%`。
- Combined 权重改为 `20 / 30 / 50 / 0`。
- 等于 Combined 实际只保留 `Sub-A`、`Sub-A-DK`、`Sub-B`，不再把 `Sub-C` 纳入组合。
- 新增 `_active_combined_strategies()` 和 `_combined_weight_label()`，让组合统计、画图、标签只针对启用中的子策略。

实际含义：

- 这是一个明显更偏向 A 股双子策略和美股轮动的版本。
- `Sub-C` 并不是被删除，而是权重设为 `0`，因此在 Combined 层被排除。

## 4. V6.4

V6.4 是变化最大、也最值得单独注意的版本。

- Sub-A 的避险资产从 `10Y国债` 切换为 `30Y国债`。
- `CN_BOND_CODE` 从 `1.H11077` 改成 `1.950175`。
- 为了支持新的官方指数取数，新增了 `CN_CSINDEX_DIRECT_CODES = {"950175"}`。
- 新增 `_is_cn_official_index_secid()`，让该指数能走正确的数据抓取路径。
- 美股最小调仓阈值从 `15%` 提高到 `25%`。
- Combined 权重恢复为 `15 / 15 / 40 / 30`，重新把 `Sub-C` 纳入组合。

实际含义：

- V6.4 的 Sub-A 避险仓位久期更长，利率敏感度高于 V6.1-V6.3。
- 美股轮动的换仓会更保守，因为必须超过 `25%` 才触发调仓。
- Combined 又回到四策略同时参与的模式。

## 逐版变化链

### V6.1 -> V6.2

只看到 Combined 权重调整：

- `15 / 15 / 40 / 30`
- 变成 `15 / 25 / 40 / 20`

### V6.2 -> V6.3

核心是 Combined 结构变化：

- `15 / 25 / 40 / 20`
- 变成 `20 / 30 / 50 / 0`

这一步相当于：

- 提高 `Sub-A`
- 提高 `Sub-A-DK`
- 提高 `Sub-B`
- 取消 `Sub-C` 在 Combined 中的权重

### V6.3 -> V6.4

这是最关键的一次切换：

- Sub-A 避险资产从 `10Y国债` 改为 `30Y国债`
- 美股最小调仓阈值从 `15%` 升到 `25%`
- Combined 权重从 `20 / 30 / 50 / 0` 恢复到 `15 / 15 / 40 / 30`

## 如果你只关心实盘影响

优先看这三件事：

1. 你是不是在用 Combined 自动分配资金。  
   因为 V6.1-V6.4 最大的连续变化，其实是 Combined 权重，而不是每次都改策略公式。

2. 你是不是在用 Sub-A 作为避险核心。  
   因为 V6.4 已经不是 `10Y国债`，而是 `30Y国债`，风险特征明显不同。

3. 你是不是在关注美股轮动换仓频率。  
   因为 V6.4 把 `US_ROT_MIN_TURNOVER` 提高到 `25%`，会减少边际调仓。

## 当前补充说明

2026-04-08 已在当前工作区同步修复一个展示层 bug：

- `V6.1` 到 `V6.4` 的 Sub-A 信号展示，之前会把国债的 R² 漏掉，导致出现“前面写持有国债，后面又写 R² 未通过持现金”的矛盾文案。
- 该问题已经统一修正，但这不属于版本间策略差异，而是展示 bug 修复。
