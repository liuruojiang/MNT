# V6.8.3 变更说明

生成日期：2026-04-22

## 版本定位

V6.8.3 基于 `mnt_bot V 6.8.2 plus.py` 生成，核心变化是把今天测试中的第 4 组方案写入正式 Sub-A：

- 保留 Sub-A 首日建仓过滤：从现金入场先买入 50%，剩余仓位等待首个日线阴线补齐。
- 新增 Sub-A 同向过热防守：权益持仓极端追涨时切到现金。

## Sub-A 新增规则

新增参数：

```python
CN_SA_SAME_SIDE_OVERHEAT_ENABLED = True
CN_SA_SAME_SIDE_OVERHEAT_ENTER = 0.36
CN_SA_SAME_SIDE_OVERHEAT_EXIT = 0.34
CN_SA_SAME_SIDE_OVERHEAT_DERISK_SCALE = 0.0
```

触发定义：

- 只对 Sub-A 当前权益持仓生效，不对现金或 10Y 国债触发。
- `bias = price / MA60 - 1`。
- `bias >= 36%` 且 `bias_mom > 0` 时，视为同向极端追涨，下一交易日有效仓位切到现金。
- `bias <= 34%` 后恢复原 Sub-A 持仓。

## 执行顺序

Sub-A 正式执行顺序为：

1. `run_cn_strategy()` 生成原始 Sub-A。
2. `apply_suba_cash_peak_decay_overlay()` 执行原有 Cash Overlay。
3. `apply_suba_same_side_overheat_overlay()` 执行新增 36/34 同向过热防守。
4. 重新计算有效持仓、交易成本、波动率缩放、净值和信号。

## 今日四组对比结论

| 方案 | 全样本 CAGR / 最大回撤 | 说明 |
|---|---:|---|
| 无首日阴线 / 无过热 | 28.05% / -25.76% | 收益略高但回撤扩大 |
| 无首日阴线 + 30/26 | 28.66% / -25.76% | 进攻性更强，回撤不改善 |
| 首日阴线 / 无过热 | 27.94% / -23.24% | 首日过滤有明显风控价值 |
| 首日阴线 + 36/34 | 28.91% / -23.24% | 四组中风险收益最平衡 |

## 验证

已执行：

```powershell
python -m py_compile 'mnt_bot V 6.8.3 plus.py'
```

并用真实 A 股数据复跑 Sub-A V6.8.3：

- 数据范围：2010-09-29 至 2026-04-22。
- Sub-A V6.8.3 全样本 CAGR：约 28.90%。
- 最大回撤：约 -23.24%。
- 36/34 过热防守触发次数：4 次。
- 过热防守覆盖天数：16 天。

## 回滚

如需回滚，直接使用 `mnt_bot V 6.8.2 plus.py`。
