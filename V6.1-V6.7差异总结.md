# V6.1-V6.7 差异总结

本文按当前工作区中的真实脚本状态整理：

- `mnt_bot V 6.1 plus.py`
- `mnt_bot V 6.2 plus.py`
- `mnt_bot V 6.3 plus.py`
- `mnt_bot V 6.4 plus.py`
- `mnt_bot V 6.5 plus.py`
- `mnt_bot V 6.6 plus.py`
- `mnt_bot V 6.7 plus.py`

注意：

1. 这是一份“当前脚本口径”的对照文档，不是历史快照复刻。
2. 其中 `US_ROT_MIN_TURNOVER` 早前已经在当前仓库里统一同步为 `0.0`，所以这里不再沿用旧文档里 `15%/25%` 的历史描述。

## 一页看懂

权重顺序固定为：

`Sub-A / Sub-A-DK / Sub-B / Sub-C`

| 版本 | Sub-A 避险资产 | Combined 权重 | ADK 风控 | 核心变化 |
|:-|:-|:-|:-|:-|
| V6.1 | 10Y 国债 | `15 / 15 / 40 / 30` | 无 | V6 系列基线 |
| V6.2 | 10Y 国债 | `15 / 25 / 40 / 20` | 无 | 提高 Sub-A-DK 权重，降低 Sub-C |
| V6.3 | 10Y 国债 | `20 / 30 / 50 / 0` | 无 | 组合层临时移除 Sub-C |
| V6.4 | 30Y 国债 | `15 / 15 / 40 / 30` | 无 | Sub-A 避险资产改 30Y，组合权重回到四策略 |
| V6.5 | 10Y 国债 | `15 / 25 / 40 / 20` | DD gate 默认开 | ADK 新增策略级净值回撤风控 |
| V6.6 | 10Y 国债 | `15 / 25 / 40 / 20` | DD gate 默认开 | Sub-A 改为“先 50% 入场，等阴线补满” |
| V6.7 | 10Y 国债 | `15 / 25 / 40 / 20` | pair-score overlay 默认开，DD gate 默认关 | ADK 正式纳入动量衰减 overlay |

## 各版本说明

### V6.1

`V6.1` 是这套四策略结构的基线版本。

- `Sub-A` 使用乖离动量 + `R²` 过滤 + 国债避险 + VolScaling。
- `Sub-A-DK` 是多配对 `Top-1` 的 A 股对冲型策略。
- `Combined` 权重是 `15 / 15 / 40 / 30`。

### V6.2

`V6.2` 的核心变化主要在组合权重，而不是单策略公式。

- `Sub-A-DK` 权重从 `15%` 提到 `25%`
- `Sub-C` 权重从 `30%` 降到 `20%`
- 其余主逻辑基本延续 `V6.1`

### V6.3

`V6.3` 是最激进的组合结构版本。

- `Combined` 权重改为 `20 / 30 / 50 / 0`
- 实际效果等于组合层暂时不再纳入 `Sub-C`
- 主变化仍集中在组合层，而不是单策略内核

### V6.4

`V6.4` 的关键点是 `Sub-A` 的避险资产切换。

- `CN_BOND_CODE` 从 `10Y 国债` 改为 `30Y 国债`
- `Combined` 权重恢复为 `15 / 15 / 40 / 30`
- 所以它更像是“恢复四策略组合，同时把 Sub-A 防守资产久期拉长”的版本

### V6.5

`V6.5` 是 `ADK` 结构第一次引入策略级风控。

新增参数：

- `CN_DK_RISK_GATE_ENABLED = True`
- `CN_DK_RISK_GATE_ENTER = 0.15`
- `CN_DK_RISK_GATE_EXIT = 0.08`
- `CN_DK_RISK_GATE_DEFENSE_SCALE = 0.5`
- `CN_DK_RISK_GATE_COOLDOWN_DAYS = 0`

主变化：

1. `Sub-A-DK` 增加 `apply_dk_drawdown_risk_gate()`
2. 触发逻辑基于 ADK 原始净值回撤，而不是 pair-score 衰减
3. `Combined` 权重同步变为 `15 / 25 / 40 / 20`

所以 `V6.5` 可以理解成：

- `ADK` 风控版
- `Sub-A-DK` 在组合里占比提高

### V6.6

`V6.6` 的主要变化不在 `ADK`，而在 `Sub-A` 执行口径。

新增参数：

- `CN_ENTRY_WAIT_DAYS = None`
- `CN_ENTRY_INITIAL_FRACTION = 0.5`

主变化：

1. `Sub-A` 从“有信号就一次性满仓”改成“先入 `50%`”
2. 剩余仓位默认只等首个日线阴线再补齐
3. `holding_fraction`、现金残仓、补仓成本都被显式建模

因此 `V6.6` 的一句话定义是：

- `Sub-A` 的分段入场版

`ADK` 本身在 `V6.6` 里仍然主要沿用 `V6.5` 的结构。

### V6.7

`V6.7` 是今天这轮研究正式落版后的版本。

新增参数：

- `CN_DK_PAIR_SCORE_DECAY_ENABLED = True`
- `CN_DK_PAIR_SCORE_DECAY_RATIO = 0.40`
- `CN_DK_PAIR_SCORE_RECOVERY_RATIO = 0.70`
- `CN_DK_PAIR_SCORE_DERISK_SCALE = 0.0`

同时调整：

- `CN_DK_RISK_GATE_ENABLED = False`

主变化：

1. `ADK` 新增 `pair-score peak-decay overlay`
2. 风控定义从“净值回撤触发”切到“信号质量衰减触发”
3. 恢复后必须先创出新的 trade peak，才允许再次触发
4. 若切换到新的 `Top-1 pair`，则视为新 trade，不继承旧 overlay 状态
5. 旧 `DD gate` 保留为可选项，但默认不再开启

所以 `V6.7` 的一句话定义是：

- `ADK` 动量衰减正式版

## 逐版演进链条

### V6.1 -> V6.2

核心是组合权重调整：

- `15 / 15 / 40 / 30`
- 改为 `15 / 25 / 40 / 20`

### V6.2 -> V6.3

核心是组合更集中：

- `15 / 25 / 40 / 20`
- 改为 `20 / 30 / 50 / 0`

### V6.3 -> V6.4

核心是 `Sub-A` 防守资产切换与组合恢复：

- 避险资产改为 `30Y 国债`
- 权重恢复到 `15 / 15 / 40 / 30`

### V6.4 -> V6.5

核心是 `ADK` 加入策略级 `DD gate`：

- `Sub-A-DK` 首次具备净值回撤触发的防守状态机
- 组合权重重新提高 `Sub-A-DK` 占比

### V6.5 -> V6.6

核心是 `Sub-A` 从一次性满仓，改成分段入场：

- 先入 `50%`
- 等阴线补满

### V6.6 -> V6.7

核心是 `ADK` 风控定义升级：

- 从“旧 DD gate 默认开启”
- 变成“pair-score 衰减 overlay 默认开启，DD gate 默认关闭”

这是一次从“净值回撤控制”向“信号衰减控制”的正式迁移。

## 如果只关心实盘影响

优先看这三件事：

1. 你是不是主要在看 `Sub-A` 执行口径。
   如果是，`V6.6` 是重要分界点，因为它改了入场方式。

2. 你是不是主要在看 `ADK` 风控定义。
   如果是，`V6.5` 和 `V6.7` 是两次关键分界：
   `V6.5` 是净值 DD gate，`V6.7` 是 pair-score 衰减 overlay。

3. 你是不是主要在看组合层默认配置。
   如果是，`V6.2/V6.3/V6.4/V6.5+` 的权重结构差异会直接影响组合净值形态。
