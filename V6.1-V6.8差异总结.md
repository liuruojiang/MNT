# V6.1-V6.8 差异总结

本文按当前工作区中的真实脚本状态整理：

- `mnt_bot V 6.1 plus.py`
- `mnt_bot V 6.2 plus.py`
- `mnt_bot V 6.3 plus.py`
- `mnt_bot V 6.4 plus.py`
- `mnt_bot V 6.5 plus.py`
- `mnt_bot V 6.6 plus.py`
- `mnt_bot V 6.7 plus.py`
- `mnt_bot V 6.8 plus.py`

注意：

1. 这是一份“当前脚本口径”的对照文档，不是历史快照复刻。
2. 表中描述优先反映各版本默认逻辑，而不是某次历史研究中的临时参数。

## 一页看懂

权重顺序固定为：

`Sub-A / Sub-A-DK / Sub-B / Sub-C`

| 版本 | Sub-A 特征 | ADK 特征 | Combined 权重 | 核心变化 |
|:-|:-|:-|:-|:-|
| V6.1 | 单段满仓切换 | 无额外风控 | `15 / 15 / 40 / 30` | V6 系列基线 |
| V6.2 | 同 V6.1 | 同 V6.1 | `15 / 25 / 40 / 20` | 提高 ADK 权重 |
| V6.3 | 同 V6.1 | 同 V6.1 | `20 / 30 / 50 / 0` | 组合层临时移除 Sub-C |
| V6.4 | 防守资产改为 30Y 国债 | 同 V6.1 | `15 / 15 / 40 / 30` | Sub-A 防守资产切换 |
| V6.5 | 回到 10Y 国债 | DD gate 默认开启 | `15 / 25 / 40 / 20` | ADK 首次加入策略级净值回撤风控 |
| V6.6 | 先入 50%，首个阴线补满 | DD gate 默认开启 | `15 / 25 / 40 / 20` | Sub-A 改为分段入场 |
| V6.7 | 保留 V6.6 分段入场 | pair-score overlay 默认开启，DD gate 默认关闭 | `15 / 25 / 40 / 20` | ADK 正式切换到信号衰减风控 |
| V6.8 | 在 V6.7 基础上再加 cash overlay | 同 V6.7 | `15 / 25 / 40 / 20` | Sub-A 正式加入 55%/90% 动量衰减切现金 |

## 各版本说明

### V6.1

`V6.1` 是这套四策略结构的基线版本。

- `Sub-A` 使用 `bias momentum + R² + bond + vol scaling`
- `Sub-A-DK` 是多配对 `Top-1` 对冲策略
- `Combined` 权重是 `15 / 15 / 40 / 30`

### V6.2

`V6.2` 的变化主要在组合层，而不是单策略公式。

- `Sub-A-DK` 权重由 `15%` 提到 `25%`
- `Sub-C` 权重由 `30%` 降到 `20%`

### V6.3

`V6.3` 是组合层最激进的版本。

- 权重改为 `20 / 30 / 50 / 0`
- 等于组合层临时不再纳入 `Sub-C`

### V6.4

`V6.4` 的关键点是 `Sub-A` 防守资产切换。

- `CN_BOND_CODE` 从 `10Y 国债` 改为 `30Y 国债`
- 组合权重恢复为 `15 / 15 / 40 / 30`

### V6.5

`V6.5` 是 `ADK` 第一次引入策略级风控的版本。

默认参数：

- `CN_DK_RISK_GATE_ENABLED = True`
- `CN_DK_RISK_GATE_ENTER = 0.15`
- `CN_DK_RISK_GATE_EXIT = 0.08`
- `CN_DK_RISK_GATE_DEFENSE_SCALE = 0.5`
- `CN_DK_RISK_GATE_COOLDOWN_DAYS = 0`

核心变化：

- `Sub-A-DK` 新增 `apply_dk_drawdown_risk_gate()`
- 风控定义基于 `ADK` 策略净值回撤

### V6.6

`V6.6` 的变化不在 `ADK`，而在 `Sub-A` 执行口径。

默认参数：

- `CN_ENTRY_WAIT_DAYS = None`
- `CN_ENTRY_INITIAL_FRACTION = 0.5`

核心变化：

- 空仓转股票时不再一次性满仓
- 先入 `50%`
- 剩余仓位默认只等首个日线阴线补满

### V6.7

`V6.7` 是 `ADK` 正式纳入 `pair-score peak-decay overlay` 的版本。

默认参数：

- `CN_DK_PAIR_SCORE_DECAY_ENABLED = True`
- `CN_DK_PAIR_SCORE_DECAY_RATIO = 0.40`
- `CN_DK_PAIR_SCORE_RECOVERY_RATIO = 0.70`
- `CN_DK_PAIR_SCORE_DERISK_SCALE = 0.0`
- `CN_DK_RISK_GATE_ENABLED = False`

核心变化：

- `ADK` 风控定义从“净值回撤触发”切到“信号衰减触发”
- 恢复后必须先创出新的 trade peak，才允许再次触发
- 新 `Top-1 pair` 视为新 trade，不继承旧过滤状态

### V6.8

`V6.8` 是在 `V6.7` 基础上，把 `Sub-A cash peak-decay overlay` 正式落版。

默认参数：

- `CN_SA_CASH_OVERLAY_ENABLED = True`
- `CN_SA_CASH_OVERLAY_DECAY_RATIO = 0.55`
- `CN_SA_CASH_OVERLAY_RECOVERY_RATIO = 0.90`

核心变化：

- 只在 `Sub-A` 持仓为股票指数时跟踪当前持仓 `bias_momentum`
- 当当前分数相对本轮 peak 衰减到 `55%` 以下时，次日切到 `cash`
- 当分数恢复到 `90%` 以上时，回到 `Sub-A` 原始状态
- 恢复后必须先创新高，才允许再次触发
- 债券持仓不受这层 overlay 干预
- 新股票持仓视为新 trade，不继承旧过滤状态

## 逐版演进链条

### V6.1 -> V6.2

组合权重调整：

- `15 / 15 / 40 / 30`
- 改为 `15 / 25 / 40 / 20`

### V6.2 -> V6.3

组合进一步集中：

- `15 / 25 / 40 / 20`
- 改为 `20 / 30 / 50 / 0`

### V6.3 -> V6.4

`Sub-A` 防守资产切换：

- 由 `10Y 国债`
- 改为 `30Y 国债`

### V6.4 -> V6.5

`ADK` 首次加入策略级 `DD gate`。

### V6.5 -> V6.6

`Sub-A` 从一次性满仓切换，改成“先 50%，首阴线补满”。

### V6.6 -> V6.7

`ADK` 从旧 `DD gate` 默认开启，切到 `pair-score overlay` 默认开启。

### V6.7 -> V6.8

`Sub-A` 在 `V6.7` 分段入场基础上，新增 `55%/90% cash overlay`。

## 如果只关心实盘影响

优先看这三件事：

1. 如果你主要关心 `Sub-A` 入场执行口径：
   `V6.6` 是关键分界点，`V6.8` 是在这个基础上再加过滤层。

2. 如果你主要关心 `ADK` 风控定义：
   `V6.5` 和 `V6.7` 是关键分界点。

3. 如果你主要关心“当前默认正式版”的结构：
   `V6.8` 是现在这条版本链里最完整的默认口径：
   `Sub-A` 用分段入场 + cash overlay，`ADK` 用 pair-score overlay。
