# V6.1-V6.9差异总结

本文只总结各版本的正式逻辑差异，不引用研究脚本的临时窗口表。

## 重要更新（2026-04-20）

- 当前正式升级版本定为 `V6.8`。
- `V6.9` 只保留为候选/实验版本，不再作为默认正式版本。
- 因此本文件中关于 `V6.9` 的内容，应理解为“实现差异说明”，不是“正式升级结论”。

## 总览
| 版本 | Sub-A | ADK | Sub-B | 组合权重 | 核心变化 |
|:-|:-|:-|:-|:-|:-|
| V6.1 | 单段满仓切换 | 无额外风控 | 原始轮动 | 15 / 15 / 40 / 30 | V6 基线 |
| V6.2 | 同 V6.1 | 同 V6.1 | 同 V6.1 | 15 / 25 / 40 / 20 | 提高 ADK 权重 |
| V6.3 | 同 V6.1 | 同 V6.1 | 同 V6.1 | 20 / 30 / 50 / 0 | 组合层临时移除 Sub-C |
| V6.4 | 防守资产改为 30Y 国债 | 同 V6.1 | 同 V6.1 | 15 / 15 / 40 / 30 | Sub-A 防守资产切换 |
| V6.5 | 回到 10Y 国债 | DD gate 默认开启 | 原始轮动 | 15 / 25 / 40 / 20 | ADK 首次加入净值回撤风控 |
| V6.6 | 先入 50%，首阴线补满 | DD gate 默认开启 | 原始轮动 | 15 / 25 / 40 / 20 | Sub-A 改为分段入场 |
| V6.7 | 保留 V6.6 | pair-score overlay 开启，DD gate 关闭 | 原始轮动 | 15 / 25 / 40 / 20 | ADK 切换到信号衰减风控 |
| V6.8 | 在 V6.7 上加入 cash overlay | 同 V6.7 | 原始轮动 | 15 / 25 / 40 / 20 | Sub-A 正式加入 55/90 cash overlay |
| V6.9 | 同 V6.8 | 同 V6.7 | 单标的 overlay 开启 | 15 / 25 / 40 / 20 | Sub-B 正式加入 25/65/0.5 asset overlay |

## 各版本要点
### V6.1
- `Sub-A`: A股 long-only
- `Sub-A-DK`: A股多空
- `Sub-B`: 美股 ETF 轮动
- `Sub-C`: 美股生产组合

### V6.2
- 组合权重从 `15 / 15 / 40 / 30` 调整为 `15 / 25 / 40 / 20`

### V6.3
- 临时把 `Sub-C` 权重降到 `0`

### V6.4
- `Sub-A` 的防守资产从 `10Y` 国债切到 `30Y` 国债

### V6.5
- `ADK` 首次加入默认开启的 `DD gate`

### V6.6
- `Sub-A` 空仓转股票时先入 `50%`
- 等首个阴线再补满

### V6.7
- `ADK` 默认风控从旧 `DD gate` 切到 `pair-score peak-decay overlay`
- 默认参数是 `40% / 70% / 0.0`

### V6.8
- `Sub-A` 正式加入股票持仓衰减后切现金
- 默认参数是 `55% / 90%`
- 债券持仓不参与这层过滤

### V6.9
- `Sub-B` 正式加入单标的 peak-decay overlay
- 默认参数是 `25% / 65% / 0.5`
- 债券腿豁免
- 被削掉的权重回到 `BIL`

## 演进链条
### V6.5 -> V6.7
`ADK` 从旧净值回撤风控，切到信号衰减风控。

### V6.6 -> V6.8
`Sub-A` 从“先入 50%，首阴线补满”继续演进到“分段入场 + cash overlay”。

### V6.8 -> V6.9
`Sub-B` 从原始轮动，演进成“原始轮动 + 单标的 peak-decay overlay”。

## 当前正式版摘要
- `Sub-A`: 分段入场 + `55/90` cash overlay
- `ADK`: `40/70/0.0` pair-score overlay
- `Sub-B`: `25/65/0.5` 单标的 overlay
- `Sub-C`: 保持既有正式逻辑不变
