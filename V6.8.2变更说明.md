# V6.8.2 变更说明

生成日期：2026-04-22

## 核心变更

- 基于 `mnt_bot V 6.8.1 plus.py` 生成 `mnt_bot V 6.8.2 plus.py`。
- Sub-B 逻辑完全继承 V6.8.1：可加杠杆资产仍只保留 `QQQ / GLD`，且 `scale > 1` 时只放大资产自身原始权重，不承接其他资产杠杆缺口。
- Sub-A-DK 新增“同向过热防守” overlay：
  - 对当前已生效 Top1 配对计算 `ratio = A / B`。
  - 计算 `bias = ratio / MA60(ratio) - 1`。
  - 仅当 `sign(bias) == sign(bias_mom)`，也就是价差乖离方向和动量方向一致时，才认为是“同向追高/追低”。
  - 当 `abs(bias) > 22%` 时，ADK 敞口降到 `0x`。
  - 当 `abs(bias) <= 18%` 或不再同向过热时，恢复正常敞口。
  - 使用 T-1 收盘可见信号判断，T 日执行，避免未来函数。
  - 敞口变化按双腿成本模型扣费：`2 * CN_COMMISSION * 当前敞口 * Δscale`。

## 新增参数

```python
CN_DK_SAME_SIDE_OVERHEAT_ENABLED = True
CN_DK_SAME_SIDE_OVERHEAT_ENTER = 0.22
CN_DK_SAME_SIDE_OVERHEAT_EXIT = 0.18
CN_DK_SAME_SIDE_OVERHEAT_DERISK_SCALE = 0.0
```

## 未变更

- Sub-A、Sub-C 不变。
- Sub-B 不变，仍为 V6.8.1 的 QQQ/GLD 可杠杆版本。
- ADK 的 Top1 配对选择、方向判断、VolScale、score-decay overlay 均不变。
- 新增防守不换配对、不改方向，只改变最终 ADK 敞口。

## 验证结果

验证脚本：

- `tmplb4zynx5/adk_drawdown_attribution_overheat_20260422/verify_v681_v682_adk.py`

真实数据源：

- EastMoney DK 价格指数
- 有效区间：`2010-11-22 ~ 2026-04-22`
- 交易日数：`3745`

关键校验：

- V6.8.1 与 V6.8.2 的 ADK raw return 最大差异：`0.0`
- Top1 配对变化天数：`0`
- 方向变化天数：`0`
- 同向过热防守触发天数：`55`
- 触发次数 / 恢复次数：`12 / 12`
- 新增防守成本合计：`2.7093%`

## ADK 回测对比

| 窗口 | V6.8.1 CAGR / 回撤 | V6.8.2 CAGR / 回撤 |
|---|---:|---:|
| 1Y | 33.90% / -15.71% | 33.90% / -15.71% |
| 3Y | 30.05% / -20.77% | 34.87% / -17.37% |
| 5Y | 33.80% / -20.77% | 36.75% / -17.37% |
| 10Y | 32.37% / -20.77% | 33.81% / -20.00% |
| 全样本 | 29.24% / -33.51% | 30.95% / -31.65% |

## 研究依据

主要研究输出：

- `tmplb4zynx5/adk_drawdown_attribution_overheat_20260422/adk_overheat_robustness_summary.csv`
- `tmplb4zynx5/adk_drawdown_attribution_overheat_20260422/adk_overheat_robustness_high_summary.csv`
- `tmplb4zynx5/adk_drawdown_attribution_overheat_20260422/v681_v682_adk_metrics.csv`
- `tmplb4zynx5/adk_drawdown_attribution_overheat_20260422/v681_v682_adk_diag.json`

结论：

- 低阈值防守是明显山谷，尤其 `8%~10%` 过早降权会大幅伤害 ADK 趋势收益。
- 稳定山脊集中在 `22%~23.5%` 触发、`17.5%~20%` 恢复、`0x/0.1x` 防守。
- `22% / 18% / 0x` 版本在全样本、3Y、5Y、10Y 都优于 V6.8.1，且 1Y 不变。

## 回滚方式

- 如需回滚，继续使用 `mnt_bot V 6.8.1 plus.py`。
- 本次改造为新增版本文件，未覆盖 V6.8.1。
