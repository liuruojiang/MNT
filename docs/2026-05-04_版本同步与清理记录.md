# 2026-05-04 版本同步与清理记录

## 修改范围

- 同步 `mnt_bot V 7.0 plus.py`、`mnt_bot V 7.1 plus.py`、`mnt_bot V 7.2 plus.py`、`mnt_bot V 7.3 plus.py` 与 `mnt_bot V 7.5 plus.py` 的适用实时确认逻辑。
- V7.0 的 `信号` 页已改为同时传入 A 股和美股实时快照。
- V7.0 / V7.1 / V7.2 的 `实时参数` 页已在第一处 Sub-B 信号日展示前执行早周信号压制。
- V7.0 / V7.1 / V7.2 / V7.3 / V7.5 的 DK 当日判断统一使用 `dk_date`，避免展示层读取口径不一致。

## 清理范围

- 删除临时测试目录：`tests/`。
- 删除 Python 编译缓存目录：`__pycache__/`。
- 删除旧临时刷新文件：`cn_v64_refresh_tmp.csv`。

## 保留范围

- 未删除正式版本脚本、正式数据 CSV、`.cn_official_cache/`、`outputs/`、`docs/`、`.codex_backups/`、历史归档和研究结论文件。
- 未清理 `.worktrees/` 内的隔离工作树和 `.vendor_libs/` 依赖目录。

## 备份

- 清理前备份目录：`.codex_backups/20260504_005913`。
- 上一轮版本同步前备份目录：`.codex_backups/20260504_003203`。

## 验证

- `python -m py_compile "mnt_bot V 7.0 plus.py" "mnt_bot V 7.1 plus.py" "mnt_bot V 7.2 plus.py" "mnt_bot V 7.3 plus.py" "mnt_bot V 7.5 plus.py"`
- 清理后不再保留仓库内测试文件，因此不再运行被删除的测试脚本。
