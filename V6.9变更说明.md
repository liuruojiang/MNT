# V6.9变更说明

## 当前状态（2026-04-20）

- `V6.8` 已定为当前正式升级版本。
- `V6.9` 不再作为默认版本，只保留为候选/实验版本。
- 本文保留 `V6.9` 的实现说明和历史回测记录，但这些内容不再代表“正式升级结论”。

## 版本定位
`V6.9` 基于 `mnt_bot V 6.8 plus.py` 形成，正式并入的是 `Sub-B` 的单标的动量衰减 overlay。

默认参数固定为：
- `decay = 25%`
- `recover = 65%`
- `derisk_scale = 0.5`

其余正式逻辑保持 `V6.8` 不变：
- `Sub-A` 继续使用先入 `50%`、首个阴线补满
- `Sub-A` 继续使用 `55% / 90%` 的 cash overlay
- `ADK` 继续使用 `40% / 70% / 0.0` 的 pair-score peak-decay overlay

## 本次正式纳入的逻辑
`Sub-B` 当前持仓中，每个非债券风险资产独立跟踪自身动量相对本轮持仓 peak 的衰减。

规则是：
1. 只对非债券风险资产生效
2. `TLT / VGLT` 债券腿不参与这层过滤
3. 某个资产衰减到阈值以下时，只削该资产
4. 被削掉的权重回到 `BIL`
5. 恢复后必须先创新高，才允许下一次再次触发
6. 新持仓视为新 trade，不继承旧过滤状态

正式接线顺序是：
1. `run_us_rotation()`
2. `apply_vol_regime_overlay()`
3. `apply_subb_asset_peak_decay_overlay()`

## 正式回测口径
本说明里的窗口指标只采用下面这套口径：
- 使用本地真实数据 `mnt_strategy_data_cn.csv` / `mnt_strategy_data_us.csv`
- 先按正式 `_fetch_data` 的同口径预处理构造 `us_rot_close` / `us_prod_daily`
- 再调用正式 `_run_strategies`

不再使用任何绕过正式预处理的 monkey patch 窗口表。

## 组合层窗口指标
按上面的正式口径，`V6.9` 组合层窗口指标为：

| 窗口 | 请求起点 | 实际起点 | 截止 | 年化收益 | 最大回撤 |
|:-|:-|:-|:-|--:|--:|
| 1Y | 2025-04-20 | 2025-04-20 | 2026-04-17 | 42.3847% | -11.5984% |
| 3Y | 2023-04-20 | 2023-04-20 | 2026-04-17 | 26.7136% | -11.5087% |
| 5Y | 2021-04-20 | 2021-04-20 | 2026-04-17 | 23.3100% | -10.8132% |
| 10Y | 2016-04-20 | 2016-04-20 | 2026-04-17 | 24.5990% | -11.4304% |
| 15Y | 2011-04-20 | 2011-04-20 | 2026-04-17 | 17.6725% | -14.5703% |

说明：
- 以上窗口表已按纠正后的正式预处理口径重算

## 一句话定义
`V6.9 = V6.8 + Sub-B single-asset peak-decay overlay (25% / 65% / 0.5, bond-exempt)`
