# V6.8 变更说明

## 版本定位

`V6.8` 基于 `mnt_bot V 6.7 plus.py` 形成，核心目标是把今天在真实本地数据上验证有效的 `Sub-A cash peak-decay overlay` 正式并入主脚本。

这次不是继续修改 `ADK`，而是在保留 `V6.7` 既有 `Sub-A` 首次入场 `50%`、等待首个日线阴线补满的执行逻辑前提下，再加一层只对股票持仓生效的动量衰减过滤。

## 本次正式纳入的改动

### 1. Sub-A 新增 cash peak-decay overlay

在 `run_cn_strategy()` 产出的 `Sub-A` 基线结果之后，新增正式风控函数：

- `_extract_active_cn_score()`
- `apply_suba_cash_peak_decay_overlay()`

正式逻辑为：

1. 只在 `Sub-A` 当前持仓为股票指数且仓位大于 `0` 时，跟踪当前持仓的 `bias_momentum`
2. 记录本轮持仓内的 `score peak`
3. 当当前 `score / peak <= 55%` 时，次日切到 `cash`
4. 当当前 `score / peak >= 90%` 时，恢复到原本 `Sub-A` 状态
5. 恢复后必须先创出新的 trade peak，才允许下一次再次触发
6. 如果 `Sub-A` 本身切到新的股票持仓，则视为新 trade，旧过滤状态不继承
7. 如果 `Sub-A` 当前持仓是债券，则这层 overlay 不介入

对应正式默认参数：

- `CN_SA_CASH_OVERLAY_ENABLED = True`
- `CN_SA_CASH_OVERLAY_DECAY_RATIO = 0.55`
- `CN_SA_CASH_OVERLAY_RECOVERY_RATIO = 0.90`

### 2. 与 V6.7 首阴线补满逻辑共存

`V6.8` 没有移除 `V6.7` 的 `Sub-A` 分段入场机制：

- `CN_ENTRY_INITIAL_FRACTION = 0.5`
- `CN_ENTRY_WAIT_DAYS = None`

也就是说，`V6.8` 的 `Sub-A` 是：

- 先按 `V6.7` 逻辑建仓
- 再叠加一层 `cash overlay`

这次正式口径不是替换，而是叠加。

### 3. ADK 沿用 V6.7 默认逻辑

`V6.8` 没有改动 `ADK` 的默认参数：

- `pair-score peak-decay overlay` 继续默认开启
- 旧 `DD gate` 继续默认关闭

所以 `V6.8` 的变化完全集中在 `Sub-A`。

## 当前实测结论

### Sub-A 层

同口径基线：

- `baseline_v67`: 年化 `23.9899%`，最大回撤 `-27.9539%`

`V6.8` 默认新参数：

- `v67 + suba cash overlay (55% / 90%)`: 年化 `27.4692%`，最大回撤 `-23.2412%`

增量为：

- 年化 `+3.4793%`
- 最大回撤改善 `+4.7127%`

分窗口表现：

- `1Y`: `62.4072% / -8.2588%`
- `3Y`: `25.6999% / -12.7840%`
- `5Y`: `27.9972% / -15.8692%`

### 整体组合层

沿用当前组合权重：

- `Sub-A 15% / Sub-A-DK 25% / Sub-B 40% / Sub-C 20%`

只把 `Sub-A` 替换成 `V6.8` 默认口径后的组合结果：

- 基线组合：年化 `14.8172%`，最大回撤 `-20.7500%`
- `V6.8` 组合：年化 `15.2506%`，最大回撤 `-20.5960%`

增量为：

- 年化 `+0.4334%`
- 最大回撤改善 `+0.1540%`

组合分窗口：

- `1Y`: `30.7168% / -12.0146%`
- `3Y`: `20.2651% / -12.0146%`
- `5Y`: `23.3260% / -12.0146%`

## 对 V6.8 的一句话定义

`V6.8 = V6.7 + Sub-A 55%/90% cash peak-decay overlay`，并保持 `V6.7` 的首阴线补满和 `ADK` pair-score overlay 默认口径不变。
