# 口径警告

本目录下 2026-04-20 早先生成的 `V6.6 / V6.7 / V6.8` 对比产物，使用了绕过正式 `_fetch_data` 预处理的导出脚本。

这意味着：
- 这些图表和汇总表不是正式 `_fetch_data + _run_strategies` 的完整口径
- 不能再作为版本优劣或窗口最大回撤的依据

处理原则：
- 原产物已移入 `已弃用_错误口径_20260420`
- 如需重建，必须按正式本地预处理口径重跑
