# V7.2 组合口径说明

生成时间：2026-04-29

## 版本定位

V7.2 不新增独立脚本，不生成 `mnt_bot V 7.2 plus.py`。

V7.2 是一个组合口径：继续维持现有两个脚本，各自输出各自的正式信号，然后在资金分配层合成。

- 主组合脚本：`mnt_bot V 7.1 plus.py`
- 微盘脚本：`PLUS 6/microcap_top100_mom16_biweekly_live.py`

提到 V7.2 时，默认含义是：以 V7.1 的 A / ADK / B 三策略为底座，再叠加微盘股策略；不再包含 Sub-C 的资金权重。

## 目标权重

V7.2 目标组合：

| 策略腿 | 来源脚本 | V7.2 权重 |
|---|---|---:|
| Sub-A | `mnt_bot V 7.1 plus.py` | 10% |
| Sub-A-DK / ADK | `mnt_bot V 7.1 plus.py` | 15% |
| Microcap / 微盘股 | `PLUS 6/microcap_top100_mom16_biweekly_live.py` | 15% |
| Sub-B | `mnt_bot V 7.1 plus.py` | 60% |
| Sub-C | 不纳入 V7.2 资金分配 | 0% |

等价写法：

```text
V7.2 = Sub-A 10% + Sub-A-DK 15% + Microcap 15% + Sub-B 60%
```

本次决策里，原先 Sub-C 的 20% 资金全部转给 Sub-B；微盘股 15% 作为独立腿保留。

## 与 V7.1 的关系

`mnt_bot V 7.1 plus.py` 代码内的原生组合权重仍是：

```python
COMBINED_WEIGHTS = {"Sub-A": 0.15, "Sub-A-DK": 0.25, "Sub-B": 0.40, "Sub-C": 0.20}
```

这代表 V7.1 自身的 Combined 展示口径，不代表 V7.2。

因此，使用 V7.2 时：

- 从 `mnt_bot V 7.1 plus.py` 读取 Sub-A / Sub-A-DK / Sub-B 的正式信号与执行口径。
- 忽略 `mnt_bot V 7.1 plus.py` 中 Sub-C 对组合资金的贡献。
- 从 `PLUS 6/microcap_top100_mom16_biweekly_live.py` 读取微盘股正式信号、成分股和调仓名单。
- 在资金层按 10 / 15 / 15 / 60 合成，而不是直接使用 V7.1 脚本输出的原生 Combined 权重表。

## 执行约定

V7.2 不是新策略脚本，而是组合资金分配约定。

实际使用时保持两个脚本：

1. `mnt_bot V 7.1 plus.py`
   - 负责 Sub-A、Sub-A-DK、Sub-B 的信号、持仓、调仓和查询展示。
   - Sub-C 可以继续作为 V7.1 内部历史功能存在，但 V7.2 资金分配中权重为 0。
2. `PLUS 6/microcap_top100_mom16_biweekly_live.py`
   - 负责微盘 Top100 动量策略的信号、成分股、进出名单和实时查询。

资金分配层按 V7.2 权重外部合并即可，不需要把微盘股逻辑并入 `mnt_bot V 7.1 plus.py`。

## 已有研究依据

既有组合测试文档：

- `docs/combo_with_microcap_weights_20260424/summary.md`
- `docs/combo_with_microcap_weights_20260424/combo_with_microcap_weights_summary.csv`
- `docs/combo_with_microcap_weights_20260424/combo_with_microcap_weights_meta.json`

该轮研究已经验证过把微盘股作为独立腿叠加到主组合的路径，并明确最大回撤应以日度 NAV 口径计算，不能只看月度组合回撤。

本次 V7.2 文档是对 2026-04-29 决策口径的固化，不是重新回测报告。

## 2026-04-29 Overlay 测试记录

当天另做了 V7.2 组合级净值回撤 overlay 测试，测试口径为：用前一日组合净值回撤触发下一日整体降仓，并计入每 100% 仓位变动 10bp 的 overlay 额外成本。

结论：暂不把该 overlay 纳入 V7.2 默认规则。

主要原因：

- V7.2 baseline 全样本日度最大回撤约为 `-7.20%`，自身回撤已经较浅。
- `4% 回撤触发 -> 80% 仓位` 可把最大回撤降到约 `-6.62%`，但年化从约 `28.59%` 降到约 `27.40%`。
- 更强的 `4% 回撤触发 -> 60% 仓位` 可把最大回撤降到约 `-6.24%`，但年化进一步降到约 `26.05%`。
- 6%/8% 触发对这条 V7.2 曲线偏晚，对最大回撤几乎没有改善。

因此，当前 V7.2 维持简单组合口径：`Sub-A 10% + Sub-A-DK 15% + Microcap 15% + Sub-B 60%`。组合级回撤 overlay 只作为后续风险工具箱候选，不作为默认生产规则。

对应留档：

- `docs/v72_combo_drawdown_overlay_20260429/summary.md`
- `docs/v72_combo_drawdown_overlay_20260429/v72_overlay_summary.csv`
- `docs/v72_combo_drawdown_overlay_20260429/v72_overlay_windows.csv`
