# 2026-04-11 评审变更记录

## 范围

本记录对应 2026-04-11 这一轮量化策略评审与修复，覆盖：

- `mnt_bot V 6.1 plus.py`
- `mnt_bot V 6.2 plus.py`
- `mnt_bot V 6.3 plus.py`
- `mnt_bot V 6.4 plus.py`
- `mnt_bot V 6.5 plus.py`
- `local_data_refresh.py`
- `归档/ADK风险闸门测试_2026-04-09/analyze_subb_risk_gate_scan.py`
- `归档/ADK风险闸门测试_2026-04-09/analyze_subc_risk_gate_scan.py`

本次没有修改 A 股 Sub-A 的当日收盘执行定义，也没有修改 Sub-C 主执行逻辑。

## 发现并处理的问题

### 1. 美股执行日只按周末跳过，没有按真实交易日历处理

问题：

- 旧逻辑把美股下一执行日、已执行状态、北京时间展示建立在“只跳过周末”的规则上。
- 节假日会导致执行时间和状态判断错位。

处理：

- 在 V6.1-V6.5 中补充真实 session 索引推导逻辑。
- `us_exec_time_str(...)` 与 `_has_execution_happened(...)` 支持基于真实 session schedule 判断。
- 美股调仓记录与展示层优先使用真实 session 索引，而不是简单 business day 推进。

影响文件：

- `mnt_bot V 6.1 plus.py`
- `mnt_bot V 6.2 plus.py`
- `mnt_bot V 6.3 plus.py`
- `mnt_bot V 6.4 plus.py`
- `mnt_bot V 6.5 plus.py`

### 2. 本地 US 面板会把 BTC 周末带进股票/ETF 研究面板

问题：

- `mnt_strategy_data_us.csv` 含 `BTC-USD` 周末行。
- 离线研究脚本如果直接 `ffill().dropna()`，会把周末伪造成股票策略 bar。

处理：

- 在 `local_data_refresh.py` 中新增 `filter_market_sessions(...)`。
- 离线扫描脚本在进入股票策略前，先按真实股票 anchor 列过滤 session，再做 `ffill`。

影响文件：

- `local_data_refresh.py`
- `归档/ADK风险闸门测试_2026-04-09/analyze_subb_risk_gate_scan.py`
- `归档/ADK风险闸门测试_2026-04-09/analyze_subc_risk_gate_scan.py`

### 3. 美股 Sub-B 回测口径与实盘不一致

问题：

- 旧版 Sub-B 回测本质上是 close-close 近似。
- 实盘口径确认是：`T 收盘信号 -> T+1 开盘执行`。

处理：

- 将 V6.1-V6.5 的 Sub-B 主执行路径统一为：
  - `T` 日收盘形成信号
  - `T+1` 日开盘成交
  - 执行日收益拆成：
    - 旧仓 `T close -> T+1 open`
    - 新仓 `T+1 open -> T+1 close`
- `run_us_rotation(...)` 正式接收 `us_open`
- `_run_strategies(...)` 传入 `self._us_open`

影响文件：

- `mnt_bot V 6.1 plus.py`
- `mnt_bot V 6.2 plus.py`
- `mnt_bot V 6.3 plus.py`
- `mnt_bot V 6.4 plus.py`
- `mnt_bot V 6.5 plus.py`

### 4. 美股 Sub-B 默认最小换手门槛统一为 0%

背景：

- 在新版 `T+1 开盘执行` 口径下，对 `US_ROT_MIN_TURNOVER` 做了真实数据扫描。
- 在未额外固化开盘冲击成本到主代码之前，`0%` 门槛在收益上表现最强，且你确认接受“几乎每周四都可能调仓”的实盘节奏。

处理：

- 将 V6.1-V6.5 的默认 `US_ROT_MIN_TURNOVER` 全部统一为 `0.0`。

影响文件：

- `mnt_bot V 6.1 plus.py`
- `mnt_bot V 6.2 plus.py`
- `mnt_bot V 6.3 plus.py`
- `mnt_bot V 6.4 plus.py`
- `mnt_bot V 6.5 plus.py`

## 本次实际做过的研究与比较

### Sub-B 旧版 vs 新版执行口径

对比对象：

- 旧版：同日信号后的 close-close 近似
- 新版：`T 收盘信号 -> T+1 开盘执行`

真实数据源：

- Yahoo 日线 `open/close`
- 资产池包含 `QQQ / TLT / GLD / EFA / EEM / DBC / BTC-USD / EMXC / BIL / SPY`

观测结论：

- 全样本下，新版总收益略高，但波动与最大回撤更高。
- 最近 5/10/15 年窗口里，新版收益更强，但风险指标不一定更优。

### Sub-B 最小换手门槛扫描

真实扫描过：

- `0%` 到 `25%`，步长 `1%`
- 分阶段比较 `0% / 10% / 25%`
- 额外叠加开盘冲击成本敏感性：`0 / 5 / 10 / 15 / 20 / 25 bps`

用途：

- 用于确认在新版执行口径下，默认门槛应如何调整。

### 开盘冲击成本敏感性

分析口径：

- 在现有 `US_ROT_COMMISSION = 0.001` 之外，再按执行日 turnover 叠加线性开盘冲击成本。
- 该项目前只用于离线敏感性分析，尚未固化进主策略代码。

## 验证

本轮实际跑过的验证包括：

- `python -m unittest tests.test_local_data_refresh`
- `python -m unittest tests.test_trading_calendars`
- `python -m unittest tests.test_local_data_refresh tests.test_trading_calendars`

其中：

- `tests.test_trading_calendars` 是本次评审期间临时新增的回归测试文件，用于锁定：
  - 真实美股节假日执行日判断
  - Sub-B 的 `T+1 open` 执行收益
  - V6.1-V6.5 默认换手门槛
- 评审结束后，按要求已删除该临时测试文件。
- 清理后重新验证：
  - `python -m unittest tests.test_local_data_refresh` 通过

## 备份

本次评审相关备份位于：

- `.codex_backups/20260411_103901`
- `.codex_backups/20260411_120702`

## 当前状态

截至本记录写入时：

- V6.1-V6.5 已统一支持 Sub-B 的 `T+1 开盘执行`
- V6.1-V6.5 已统一将 `US_ROT_MIN_TURNOVER` 设为 `0.0`
- 本地 US 股票研究面板已规避 BTC 周末伪 bar
- 评审期临时测试文件已清理

## 未纳入本轮代码改动的事项

- 开盘冲击成本尚未正式写入主回测逻辑，只做了敏感性分析
- 代码里仍有 `pct_change` 的 `FutureWarning`，属于未来 pandas 兼容性问题，不是本轮阻塞性问题
